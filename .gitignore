# Transient epic-decomposition doc emitted by /specify (deleted after generating bd issues)
.spec.md

# bd (Beads) backlog — belongs to the working project, not this repo
.beads/

# OS / editor cruft
.DS_Store
*.swp

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
